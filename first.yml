---
  - name: "My first play"
    hosts: localhost

    tasks:
    
    

      - name: "test reachabiliy"
        ping:

      - name: "install stress"
        homebrew:
          name: stress
          state: present
